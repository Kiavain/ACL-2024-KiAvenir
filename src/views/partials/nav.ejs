<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
    <div class="container-fluid">
        <!-- Brand (Optional) -->
        <a class="navbar-brand d-flex align-items-center" href="/">
            <!-- Logo on the left -->
            <img src="/img/favicon.ico" alt="Logo" width="30" height="30" class="d-inline-block align-text-top">
            <!-- Brand name -->
            <span class="ms-2">KiAvenir</span> <!-- Add some margin-left with 'ms-2' -->
        </a>

        <!-- Toggle button for mobile view -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Left links and right dropdown -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <!-- Left-aligned links -->
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link <%= currentPath === '/' ? 'active' : '' %>" aria-current="page" href="/">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= currentPath === '/agenda' ? 'active' : '' %>" href="/agenda">Agenda</a>
                </li>
            </ul>

            <!-- Right-aligned dropdown -->
            <ul class="navbar-nav">
                <% if (locals.user) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <!-- Logo on the left -->
                            <img src="/img/account.jpg" alt="Logo" width="30" height="30" class="d-inline-block align-text-top">
                            <!-- Brand name -->
                            <span class="ms-2"><%= locals.user.username %></span> <!-- Add some margin-left with 'ms-2' -->
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item <%= currentPath === '/account' ? 'active' : '' %>" href="/account">Mon compte</a></li>
                            <li><a class="dropdown-item <%= currentPath === '/agenda' ? 'active' : '' %>" href="/agenda">Mes agendas</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/account/logout">Déconnexion</a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <span class="nav-separator"></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/signin">Inscription</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>