<!DOCTYPE html>
<html lang="fr">
<head>
    <title>KiAvenir - Accueil</title>
</head>
<body>
<% /* global locals */ %>
<%- include('partials/header') %>

<div class="container"
     style="display: flex; justify-content: center; align-items: center; height: 25%; flex-direction: column;">
    <% if (locals.user) { %>
        <h1 class="text-center fw-bold text-primary">Bonjour, <%= locals.user.username %>!</h1>
    <% } else { %>
        <h1 class="text-center fw-bold text-primary">KiAvenir</h1>
    <% } %>
    <h3 class="text-center mt-4">
        <% if (locals.user) { %>
            <div>
                <span class="fw-bold">Voici vos prochains évenements à venir:</span>
                <div id="homepageDailyPreview"></div>
            </div>
        <% } else { %>
            <div class="mb-3 fs-4">
                Prévoir l'avenir avec KiAvenir !
            </div>
            <div class="mb-2 fs-6">
                Vous pouvez dès à présent <a href="/signin" class="link-underline link-underline-opacity-0">créer un
                    compte</a> ou <a href="/login" class="link-underline link-underline-opacity-0">vous connecter</a>
                pour accéder à toutes les fonctionnalités.
            </div>
        <% } %>
    </h3>
</div>
<script type="module" src="/js/homepage/homepage.js"></script>
<%- include('partials/footer') %>
</body>
</html>
