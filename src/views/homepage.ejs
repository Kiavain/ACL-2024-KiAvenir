<!DOCTYPE html>
<html lang="fr">
<head>
  <title>KiAvenir - Accueil</title>
</head>
<body>
    <% /* global locals */ %>
    <%- include('partials/header') %>

    <div class="container" style="display: flex; justify-content: center; align-items: center; height: 25%; flex-direction: column;">
      <h1 class="text-center fw-bold text-primary">Page d'accueil</h1>
      <h3 class="text-center mt-4">
        <% if (locals.user) { %>
          <div>
            <h4 class="fst-italic">Bienvenue <span class="fw-bold"><%= locals.user.username %></span> !</h4>
          </div>
          <div class="fs-5">
            N'hésitez pas à visiter les autres pages de notre magnifique site de gestion d'agendas.
          </div>
          <% } else { %>
            <div class="mb-3 fs-4">
              Bienvenue sur notre site de gestion d'agendas.
            </div>
            <div class="mb-2 fs-6">
              Vous pouvez dès à présent <a href="/signin" class="link-underline link-underline-opacity-0">créer un compte</a> ou <a href="/login" class="link-underline link-underline-opacity-0">vous connecter</a> pour accéder à toutes les fonctionnalités.
            </div>
        <% } %>
      </h3>
    </div>

    <%- include('partials/footer') %>
</body>
</html>
